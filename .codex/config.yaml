setup:
  - pnpm install

commands:
  test: pnpm test
  lint: pnpm lint
  format: pnpm format:check

include:
  - 'services/**'
  - '*.config.js'
  - 'package.json'
  - 'pnpm-workspace.yaml'

exclude:
  - 'node_modules/**'
  - 'coverage/**'
  - 'pnpm-lock.yaml'
  - '.git/**'
